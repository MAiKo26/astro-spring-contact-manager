---
import "../styles/globals.css";
import Layout from "../layouts/Layout.astro";
import NewContact from "../components/AddContactForm.astro";
import ContactListFetch from "../components/ContactListFetch.astro";

const API = "http://localhost:8080/contacts";

const response = await fetch(API);
const data = await response.json();
console.log(data.content.map(({id}) => id));
---

<Layout><ContactListFetch data={data.content} /></Layout>
